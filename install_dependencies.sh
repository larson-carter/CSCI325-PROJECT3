#!/bin/bash

# Update package lists
sudo apt-get update

# Install wget
sudo apt-get install -y wget

# Install nmap
sudo apt-get install -y nmap

echo "wget and nmap have been installed successfully."
